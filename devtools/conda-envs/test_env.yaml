name: paprika-dev
channels:
  - conda-forge
  - omnia
  - ambermd
dependencies:
  
  # Base depends
  - python
  - pip
  - numpy
  - scipy
  - parmed
  - mdtraj
  - pymbar
  - openmm
  - ambertools=19
  - pyyaml
  
  # Documentation
  - sphinx
  - sphinx_rtd_theme

  # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
