name: paprika-dev
channels:
  - conda-forge
  - ambermd
  - omnia
dependencies:
  
  # Base depends
  - python
  - pip
  - numpy
  - scipy
  - parmed
  - mdtraj
  - pymbar==3.0.3
  - openmm
  - ambertools=19
  - pyyaml
  
  # Documentation
  - sphinx
  - sphinx_rtd_theme

  # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
