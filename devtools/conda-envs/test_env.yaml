name: paprika-dev
channels:
  - conda-forge
  - omnia
dependencies:
  
  # Base depends
  - python
  - pip
  - numpy
  - scipy
  - parmed
  - mdtraj
  - pymbar >=3.0.5
  - openmm
  - ambertools >=20.0
  - pyyaml
  
  # Documentation
  - sphinx
  - sphinx_rtd_theme

  # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
