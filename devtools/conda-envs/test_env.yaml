name: paprika-dev
channels:
  - conda-forge
  - omnia
  - ambermd
dependencies:
  
  # Base depends
  - python
  - pip
  - numpy
  - scipy
  - parmed
  - mdtraj
  - pymbar
  - openmm
  - ambertools=18
  
  # Documentation
  - sphinx
  - sphinx_rtd_theme

  # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
