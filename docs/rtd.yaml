name: test

channels:
  - omnia

dependencies:
  
  # Base depends
  - python
  - pip
  - numpy
  - scipy
  - parmed

  
  # Documentation
  - sphinx
  - sphinx_rtd_theme

  # Pip-only installs
  - pip:
    - pytraj
  
